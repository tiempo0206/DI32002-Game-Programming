/// <summary>
/// Teams that can own painted ground cells.
/// Keep this enum small for the MVP so scoring and debugging stay simple.
/// </summary>
public enum Team
{
    None = 0,
    TeamA = 1,
    TeamB = 2
}
